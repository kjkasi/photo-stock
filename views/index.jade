extends layout

block content
  .jumbotron
    if (!user)
      a(href="/login") Login
      br
      a(href="/register") Register
    if (user)
      p Welcome #{user.username}
      br
      a(href="/logout") Logout
